# TypeScript Week One Assignment

This is a simple implementation of a grocery list using TypeScript and HTML.

## Setup

To compile the TypeScript to JavaScript, run this command in the terminal:
`tsc grocery.ts`
